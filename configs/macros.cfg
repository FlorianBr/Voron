#####################################################################
#   Macros
# Sources:
# Source: https://github.com/jontek2/A-better-print_start-macro
# https://docs.vorondesign.com/community/macros/macros/120decibell.html
#####################################################################


# Auto-Shutdown printer when done and cold
[delayed_gcode POWER_OFF_PRINTER_CHECK]
gcode:
  RESPOND PREFIX="info" MSG="Check for Auto-Off ..."
  {% if printer.idle_timeout.state == "Idle" or printer.idle_timeout.state == "Ready" %}
    {% if printer.extruder.temperature < 50.0 and printer.heater_bed.temperature < 65.0 %}
      {% if printer.extruder.target == 0.0 and printer.heater_bed.target == 0.0 %}
        UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=0
        RESPOND PREFIX="info" MSG="Auto-Off!"
        UPDATE_DELAYED_GCODE ID=POWER_OFF_PI DURATION=30
      {% else %}
        RESPOND PREFIX="info" MSG="Retry Auto-Off, Target-Temps set"
        UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=30
      {% endif %}
    {% else %}
      {% if printer.idle_timeout.state == "Printing" %}
        RESPOND PREFIX="info" MSG="Aborting Auto-Off, printing!"
        UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=0
      {% else %}
        {% if printer.extruder.target == 0.0 and printer.heater_bed.target == 0.0 %}
          RESPOND PREFIX="info" MSG="Retry Auto-Off, Target-Temps still set"
          UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=30
        {% else %}
          RESPOND PREFIX="info" MSG="Retry Auto-Off, too warm"
          UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=30
        {% endif %}
      {% endif %}
    {% endif %}
  {% endif %}

# Power off Raspi
[delayed_gcode POWER_OFF_PI]
gcode:
    SHOW_OFF
    {action_call_remote_method("shutdown_machine")}
initial_duration: 0.0

[gcode_macro CG28]
description: Conditional G28 Home if not homed
gcode:
    {% if "x" not in printer.toolhead.homed_axes or "y" not in printer.toolhead.homed_axes or "z" not in printer.toolhead.homed_axes %}
    G28
    {% endif %}

[gcode_macro SHOW_READY]
description: Show READY Status
gcode:
    SET_LED LED=hotend_rgb RED=0 GREEN=1 BLUE=0 INDEX=1
    SET_LED LED=case_rgb RED=1 GREEN=1 BLUE=1

[gcode_macro SHOW_WORKING]
description: Show WORKING Status
gcode:
    SET_LED LED=hotend_rgb RED=1 GREEN=0.5 BLUE=0 INDEX=1
    SET_LED LED=case_rgb RED=1 GREEN=1 BLUE=1

[gcode_macro SHOW_PRINTING]
description: Show PRINTING Status
gcode:
    SET_LED LED=hotend_rgb RED=0 GREEN=0 BLUE=0 INDEX=1
    SET_LED LED=case_rgb RED=1 GREEN=1 BLUE=1

[gcode_macro SHOW_ERROR]
description: Show ERROR Status
gcode:
    SET_LED LED=hotend_rgb RED=1 GREEN=0 BLUE=0 INDEX=1
    SET_LED LED=case_rgb RED=1 GREEN=0 BLUE=0

[gcode_macro SHOW_OFF]
description: Show OFF Status
gcode:
    SET_LED LED=hotend_rgb RED=0 GREEN=0 BLUE=0 INDEX=1
    SET_LED LED=case_rgb RED=0 GREEN=0 BLUE=0

[gcode_macro GOTO_PARK]
description: Go to PARK position
gcode:
    G91                         ; Relative Positioning
    G0 Z20 F10000               ; Up
    G90                         ; Absolute Positioning
    G0 X285 Y330 Z30 F20000     ; Move to bucket

[gcode_macro GOTO_CENTER]
description: Go to CENTER position
gcode:
    G90                         ; Absolute Positioning
    G0 X160 Y160 Z50 F20000     ; Move to center


[gcode_macro WIPE_NOZZLE]
description: Wipe the nozzle
gcode:
    G0 X230 Y330 Z20 F10000     ; Move to brush
    G0 X230 Y330 Z5 F10000      ; Down
    G0 X270 Y330 Z5 F10000      ; Wipe right
    G0 X230 Y330 Z5 F10000      ; Wipe left
    G0 X270 Y330 Z5 F10000      ; Wipe right
    G0 X230 Y330 Z5 F10000      ; Wipe left
    G0 X270 Y330 Z5 F10000      ; Wipe right
    G0 X230 Y330 Z5 F10000      ; Wipe left
    G0 X230 Y330 Z20 F10000     ; Up

[gcode_macro G32]
description: Full homing
gcode:
    M117 Homing
    SHOW_WORKING
    CG28                        ; Home if not homed to get everything turned on
    M117 QGL
    QUAD_GANTRY_LEVEL           ; Level
    M117 Re-Homing
    G28 X Y                     ; Home the X and Y
    WIPE_NOZZLE
    G28 Z                       ; Home the Z now that nozzle is clean
    G0 Z20                      ; Raise nozzle
    GOTO_PARK
    M117 Ready
    SHOW_READY

[gcode_macro PRINT_START]
description: START of print
gcode:
    # Parameters
    {% set BED_TEMP = params.BED|float %}
    {% set EXTRUDER_TEMP = params.EXTRUDER|float %}
    {% set BED_MESH = params.BED_MESH|default('adaptive')|string %} ; One of: adaptive (default), full, default (or any other value as the bed mesh profile name), none
    BED_MESH_CLEAR    
    SAVE_GCODE_STATE NAME=start
    SHOW_WORKING
    M117 Warmup
    M140 S{BED_TEMP}                    ; set bed temp
    M104 S150                           ; set hot end homing temp
    M190 S{BED_TEMP}                    ; wait for bed to reach temp
    M109 S150                           ; hot end to reach homing temp
    G32                                 ; Full homing
    {% if BED_MESH == 'full' %}         ; Generate Bed Mesh
    BED_MESH_CALIBRATE
    {% elif BED_MESH == 'adaptive' %}
    BED_MESH_CALIBRATE ADAPTIVE=1
    {% elif BED_MESH != 'none' %}
    BED_MESH_PROFILE LOAD={BED_MESH}
    {% endif %}
    M117 Heating up
    M104 S{EXTRUDER_TEMP}               ; set hot end target temp
    GOTO_PARK
    M83                                 ; Make the E relative independant of other axis
    M109 S{EXTRUDER_TEMP}               ; set and wait for hot end to reach temp
    GOTO_CENTER
    LINE_PURGE                          ; KAMP Line Purge near print
    RESTORE_GCODE_STATE NAME=start
    SHOW_PRINTING
    M117 Printing
    GOTO_CENTER


[gcode_macro PRINT_END]
description: END of print
gcode:
    M400                           ; wait for buffer to clear
    G92 E0                         ; zero the extruder
    G1 E-10.0 F3600                ; retract filament
    G91                            ; relative positioning
    G0 Z1.00 X20.0 Y20.0 F20000    ; move nozzle to remove stringing
    TURN_OFF_HEATERS
    M107                           ; turn off fan
    G1 Z10 F3000                   ; move nozzle up
    G90                            ; absolute positioning
    GOTO_PARK
    BED_MESH_CLEAR
    M117 Done!
    UPDATE_DELAYED_GCODE ID=POWER_OFF_PRINTER_CHECK DURATION=120 ; OFF Makro with 120s delay
    SHOW_READY

[gcode_macro CANCEL_PRINT]
description: CANCEL a print
gcode:
    M117 Cancelled!
    TURN_OFF_HEATERS
    M106 S100       # Part Fan full speed
    M18             # Turn off motors
    SHOW_ERROR
